from django.contrib import admin
# from .models import StoreData

# class StoreDataAdmin(admin.ModelAdmin):
#     list_display = ('store_hash', 'access_token', 'user_email','created_at','updated_at')  # Fields to display in the list view
#     search_fields = ('user_email',)  # Add search functionality for the 'name' field

# admin.site.register(StoreData, StoreDataAdmin)